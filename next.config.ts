import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Vercel deployment is zero-configuration for Next.js
  // No additional configuration needed
};

export default nextConfig;
